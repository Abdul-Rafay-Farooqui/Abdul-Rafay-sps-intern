/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://nepra.netlify.app", // your deployed site URL
  generateRobotsTxt: true, // also generates robots.txt
  sitemapSize: 5000, // optional, splits large sitemaps
};
